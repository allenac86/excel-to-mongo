class Book {
	constructor(title, author, series, numInSeries, isRead) {
		this.title = title;
		this.author = author;
		this.series = series;
		this.numInSeries = numInSeries;
		this.isRead = isRead;
	}
}

module.exports = Book;
